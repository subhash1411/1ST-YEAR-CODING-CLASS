#include<stdio.h>
int main()
{
	int n,*ptr,i,sum=0;
	printf("Enter number of elements:");
	scanf("%d",&n);
	ptr=(int* )malloc(n*sizeof(int));
	for(i=0;i<n;i++)
	{
		scanf("%d",ptr+i);
			sum+=*ptr+i;
	}
	printf("Sum of given values : %d",sum);
}
